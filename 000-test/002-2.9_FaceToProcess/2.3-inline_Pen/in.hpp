#pragma once
#include <iostream>
#include <string>
#include <vector>


inline bool inrange(const int n) { return (n >= 1 && n <= 1024); }
bool Pentagonal(const int, std::vector<int> &);
void Print_vector(std::vector<int> &, std::string);